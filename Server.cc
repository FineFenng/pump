#include <iostream>

#include <sys/socket.h>
#include <unistd.h>

using namespace std;


int main()
{

    int fd;
    struct socket_in serverAddr;
    struct socketaddr_in

    fd = ::socket(AF_INET, SOCK_STREAM, 0);

    return 0;




}

